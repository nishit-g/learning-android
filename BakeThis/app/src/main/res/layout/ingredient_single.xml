<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="14sp">

    <TextView
        android:id="@+id/tv_ingredients"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="This is a list of ingredients"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:padding="4sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/horizontal_line"
        app:layout_constraintEnd_toStartOf="@id/tv_quantity_and_measure"
        app:layout_constraintHorizontal_bias="0"/>

    <TextView
        android:id="@+id/tv_quantity_and_measure"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="8 tblsp"
        android:textSize="20sp"
        android:textColor="@color/yellowOrange"
        android:textStyle="bold"
        android:padding="4sp"
        app:layout_constraintStart_toEndOf="@id/tv_ingredients"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1"
        app:layout_constraintBottom_toBottomOf="@id/horizontal_line"/>

    <View
        android:id="@+id/horizontal_line"
        android:layout_width="match_parent"
        android:background="@color/black"
        android:layout_height="1dp"
        android:layout_marginRight="8sp"
        android:layout_marginBottom="8sp"
        app:layout_constraintTop_toBottomOf="@id/tv_quantity_and_measure"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="8sp" />

</androidx.constraintlayout.widget.ConstraintLayout>